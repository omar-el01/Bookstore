{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
   <div class="container-fluid p-4 ">
<div class="row">
<div class="col-md-2"></div>
<div class="col-md-8  p-3  bg-light text-center border border-5">
    <h3 class="mb-4 text-center">genre details :</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ genre.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genre_index') }}">back to list</a>

    
    </div></div></div>

{% endblock %}
